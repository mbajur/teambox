<turbo-stream action="update" target="<%= dom_id(@conversation, :errors) %>">
  <template>
    <div class='errors_for'><span class="error"><%= @conversation.errors.full_messages.to_sentence %></span></div>
  </template>
</turbo-stream>
