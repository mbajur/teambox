- @title = "#{h @page} - #{h @project}"

%h2=h @page

= @page.content

.log_in_to_reply
  = link_to "Log into Teambox", project_page_path(@project, @page)
  to edit this page.

= render 'public/projects/column'
